pandas>=2.0,<3.0
numpy>=1.26,<3.0
# Pinned exactly: sklearn's pickle format is not guaranteed stable across
# versions (observed: a 1.7.2-trained pipeline failed to unpickle under 1.8.0
# with "STACK_GLOBAL requires str"). models/*.pkl is generated locally/in CI
# by `make train` (not committed to git), so keeping every environment on the
# same sklearn version avoids producing artifacts that can't be loaded back in
# a different environment. If you deliberately upgrade, retrain and re-pin.
scikit-learn==1.7.2
matplotlib>=3.8,<4.0
seaborn>=0.13,<0.14
jupyter>=1.0,<2.0
scipy>=1.11,<2.0
joblib>=1.3,<2.0
